# User Profile - tyeth

## Preferences
- Prefers concise and direct responses.
- Prefers automated setups and robust scripts (TDD).
- Active development is performed on a local Windows host with files residing in a WSL instance (Ubuntu).
